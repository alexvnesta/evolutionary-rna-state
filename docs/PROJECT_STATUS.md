# Project status — cross-session coordination snapshot

_Snapshot: 2026-07-08 18:00 UTC. Generated by the coordination session (`837512d2`)._

**8 of 14 sessions completed; 6 processing; 0 awaiting user approval.**

## Sessions

| Session | Status | Role |
|---|---|---|
| Raw RNA-seq Feature Fine-tuning (`837512d2`) | processing | THIS session — cross-session coordination, roadmap, commits, monitoring; staged liu2019 expression matrix. |
| Latest Literature Search Update (`e6a36497`) | processing | Full-cohort alignments + RNA-editing/AEI (16 samples); Recoding Editing Index. |
| Evaluate Orthrus for Genome Analysis (`15defe54`) | processing | Evaluating Orthrus (mature-RNA foundation model) as encoder; scVI projection of cohort + IMvigor210. |
| Find/Create Nextflow RNA-seq Pipelines (`c15a540e`) | processing | Raw-read pipeline (arm64). HISAT2 spine + custom subworkflows (incl. te_erv) verified on pilot; rnasplice pilot running. |
| Clonal Evolution Trajectory Inference Pipeline (`b41a2d10`) | processing | Clonal-trajectory multi-track (cSCC/DDLPS/AML), malignant-compartment features. |
| Quantify 16 Un-quantified Hugo RNA Runs (`55f2530f`) | processing | Hugo cohort RNA quant; full-depth batch. |
| Clean Up Repository Root (`99462c38`) | completed | Repo-root hygiene. COMPLETED. |
| Obtain SRA/iAtlas ID Crosswalk (`83645bba`) | completed | gide2019 crosswalk + hypothesis kill-tests; OWNS the liu2019 auto-runner. |
| Built with Claude Life Sciences Hackathon (`f16431e3`) | completed | Manuscript (HTML), cartoons, deliverables. COMPLETED. |
| Immunotherapy Response Prediction Model (`e51c751a`) | completed | Interpretable backbone + antigen arms + eval harness. COMPLETED. |
| Daily Code Cleanliness Monitor Automation (`a514ce22`) | completed | Repo-hygiene automation. COMPLETED. |
| Literature Review: Immunotherapy Response Modeling (`71890512`) | completed | Field review; published rbp-activity-scorer skill. COMPLETED. |
| Hackathon Compliance Boundary Review (`3b7dadbb`) | completed | Compliance boundary. COMPLETED. |
| User Uploads (`307bb791`) | completed | Uploaded inputs. |

## Repository (`github.com/alexvnesta/evolutionary-rna-state`, `main`)
Git metadata in `.gitmeta/`; drive with `GIT_DIR=.gitmeta GIT_WORK_TREE=. git ...`.
`.gitignore` excludes vendored `pipelines/nfcore/`, `results/`, BAMs, parquet,
Nextflow scratch. HEAD == remote; all sessions self-commit their own trees.

## Scientific bottom line (session `83645bba`, `docs/FINAL_VERDICT.md`)
Core hypothesis — tumor-intrinsic "evolutionary RNA state" predicts ICB response —
does NOT survive at 3 cohorts (n=66). Learned RNA representation at chance
(LOCO AUROC 0.49, perm p=0.41); immune floor real-but-weak (0.59, p=0.044).
Auto-runner (`analysis/kill_tests/move2_autorun.py`) re-fires all kill-tests when the
powered liu2019 cohort (~n180, 4th held-out) lands. Clonal-trajectory arm reports a
positive grouped-CV DDLPS signal (AUC 0.940, p=0.005) — needs replication.

## liu2019 adjudication — expression matrix now STAGED (coordination session)
The decisive powered test was blocked on the liu2019 expression matrix. It has been
fetched from cBioPortal and staged as artifacts (NOT yet wired into the auto-runner):
- `liu2019_cbioportal_rna_seq_mrna.csv` (artifact `2df0f265`) — 122 samples x 39,168
  genes, from study `mel_iatlas_liu_2019`, profile `..._rna_seq_mrna` (CONTINUOUS
  "Expression levels"). Sample IDs `Liu_SampleN` match the existing clinical tsv.
- `liu2019_cbioportal_README.md` (artifact `2ebd0e18`) — full provenance + the
  reconciliation needed.
**Reconciliation needed before it feeds the adjudication** (owner: crosswalk `83645bba`):
the auto-runner expects `quant_gene_tpm.parquet` shaped SAMPLES x `ENSG` columns in
TPM keyed by `run_accession`+`cohort`; the staged matrix is genes x samples,
Entrez/Hugo-keyed, in cBioPortal expression units (NOT TPM). Either remap onto the
pipeline TPM space / ENSG convention, or run liu2019 raw reads through the same
salmon/HISAT2 path (cleanest for apples-to-apples LOCO). Do NOT drop the CSV in as
`quant_gene_tpm.parquet` — units mismatch would pollute the multi-cohort matrix.

## Verified pipeline milestones
- HISAT2 alignment spine validated (pilot PD1_35_PRE: 79.08M reads, 100% mapped/paired).
- RNA editing: real output on 16 samples across both cohorts (AEI tables, signal/noise-guarded).
- te_erv (Telescope) end-to-end run independently verified on disk (task exit 0,
  34.7M fragments, 5,462 TE/ERV transcripts non-zero). rnasplice/rnafusion still
  configured-but-not-run-on-real-data.

## Items needing user action
- None open.

# Project status — cross-session coordination snapshot

_Snapshot: 2026-07-08 03:26 UTC. Generated by the coordination session; refreshed on request, not authoritative between refreshes._

## Active agents

| Session | Status | Role / current focus |
|---|---|---|
| Obtain SRA/iAtlas ID Crosswalk (`83645bba`) | processing | gide2019 SRA<->iAtlas crosswalk; plan approved, running head-to-head benchmark. |
| Raw RNA-seq Feature Fine-tuning (`837512d2`) | processing | THIS session — cross-session coordination, roadmap, commits, monitoring. |
| Built with Claude Life Sciences Hackathon (`f16431e3`) | processing | Hackathon coordination / manuscript & figures. |
| Latest Literature Search Update (`e6a36497`) | processing | Ongoing literature acquisition. |
| Clonal Evolution Trajectory Inference Pipeline (`b41a2d10`) | processing | Clonal-evolution trajectory method + matched-cohort feasibility. |
| Find/Create Nextflow RNA-seq Pipelines (`c15a540e`) | processing | Raw-read pipeline (arm64). STAR->HISAT2 pivot; pilot BAM verified (PD1_35_PRE 79.08M reads, 100% mapped/paired). Also debugging intron-retention integration test. |
| Daily Code Cleanliness Monitor Automation (`a514ce22`) | awaiting user response | Repo-hygiene automation. |
| Immunotherapy Response Prediction Model (`e51c751a`) | processing | Interpretable latent-state + response backbone and RNA-phenotype antigen arms. Now self-committing analysis/, src/, results/. |
| Quantify 16 Un-quantified Hugo RNA Runs (`55f2530f`) | processing | Completing Hugo cohort RNA quantification (16 missing runs) for full 3-cohort coverage. |
| Parked:  Literature Review: Immunotherapy Response Modeling (`71890512`) | completed |  |
| Parked: Hackathon Compliance Boundary Review (`3b7dadbb`) | completed |  |
| User Uploads (`307bb791`) | completed | Uploaded inputs. |

## Repository state (`github.com/alexvnesta/evolutionary-rna-state`, branch `main`)

Git metadata is in `.gitmeta/` (sandbox workaround): drive with
`GIT_DIR=.gitmeta GIT_WORK_TREE=. git ...`; rename to `.git` on a normal machine.
HEAD `d1d830a` as of snapshot.

**Sessions are now committing their own trees** — since the coordination commits,
the modeling and pipeline sessions landed the first-party Nextflow arm, phenotype
antigen arms, pilot Salmon analysis, reproducibility scaffolding, and docs. No
bulk-commit by the coordination session is needed; each owner commits at its own
checkpoints. Coordination session commits only its own docs (ROADMAP,
PROJECT_STATUS).

## Verified milestone
- **HISAT2 pilot alignment confirmed.** sample PD1_35_PRE -> valid BAM,
  79,080,188 reads, 100% mapped, 100% properly paired (full-depth). Resolves the
  arm64 STAR empty-BAM failure; HISAT2 is the alignment path. BAMs live under the
  git-ignored `results/large/` (correctly not committed).

## Items needing user action
- **Daily Code Cleanliness Monitor Automation** (awaiting user response): Confirming which cleanup tiers to run

## Notes
- STAR-under-Rosetta failure claim: the Nextflow session was independently
  re-verifying its STAR-version/Rosetta assertions during this window; treat that
  specific claim as that session's to confirm. HISAT2 pivot is verified here.

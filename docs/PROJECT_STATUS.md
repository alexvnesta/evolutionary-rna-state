# Project status — cross-session coordination snapshot

_Snapshot: 2026-07-08 03:38 UTC. Generated by the coordination session; refreshed on request._

## Active agents

| Session | Status | Role / current focus |
|---|---|---|
| Built with Claude Life Sciences Hackathon (`f16431e3`) | completed | Hackathon coordination / manuscript & figures. COMPLETED. |
| Raw RNA-seq Feature Fine-tuning (`837512d2`) | processing | THIS session — coordination, roadmap, commits, monitoring. |
| Find/Create Nextflow RNA-seq Pipelines (`c15a540e`) | processing | Raw-read pipeline (arm64). STAR->HISAT2 pivot; pilot BAM verified (PD1_35_PRE 79.08M reads, 100% mapped/paired). Also intron-retention module. |
| Latest Literature Search Update (`e6a36497`) | processing | Ongoing literature acquisition. |
| Clonal Evolution Trajectory Inference Pipeline (`b41a2d10`) | processing | Clonal-evolution trajectory + matched-cohort feasibility. |
| Immunotherapy Response Prediction Model (`e51c751a`) | completed | Interpretable backbone + phenotype antigen arms. COMPLETED; committed its analysis/, src/, results/ (commit 68a2b63). Now monitoring for de-novo antigen matrices from pipeline sessions. |
| Obtain SRA/iAtlas ID Crosswalk (`83645bba`) | processing | gide2019 crosswalk; plan approved, head-to-head benchmark running. |
| Daily Code Cleanliness Monitor Automation (`a514ce22`) | processing | Repo-hygiene automation. |
| Quantify 16 Un-quantified Hugo RNA Runs (`55f2530f`) | processing | Completing Hugo cohort RNA quant (16 runs) for full 3-cohort coverage. |
| Parked:  Literature Review: Immunotherapy Response Modeling (`71890512`) | completed |  |
| Parked: Hackathon Compliance Boundary Review (`3b7dadbb`) | completed |  |
| User Uploads (`307bb791`) | completed | Uploaded inputs. |

## Repository state (`github.com/alexvnesta/evolutionary-rna-state`, branch `main`)
Git metadata in `.gitmeta/` (sandbox workaround); drive with
`GIT_DIR=.gitmeta GIT_WORK_TREE=. git ...`. HEAD `68a2b63` as of snapshot;
local == remote.

**All sessions commit their own trees.** The completed modeling session committed
its full `analysis/`, `src/`, `results/` source before finishing — nothing was
left uncommitted. The only outstanding working-tree changes are `README.md` and
`docs/manuscript.html`, modified by live hackathon/manuscript sessions (theirs to
commit). No bulk-commit by the coordination session is needed.

## Verified milestone
- **HISAT2 pilot alignment confirmed** — PD1_35_PRE -> valid BAM, 79,080,188
  reads, 100% mapped, 100% properly paired (full-depth). Resolves the arm64 STAR
  empty-BAM failure. BAMs under git-ignored `results/large/` (correctly excluded).

## Items needing user action
- None currently.

## Notes
- STAR-under-Rosetta claim is the Nextflow session's to confirm; HISAT2 pivot is
  verified here independently.

# Project status — cross-session coordination snapshot

_Snapshot: 2026-07-08 02:42 UTC. Generated by the coordination session; refreshed on request, not authoritative between refreshes._

## Active agents

| Session | Status | Last active | Role / current focus |
|---|---|---|---|
| Raw RNA-seq Feature Fine-tuning (`837512d2`) | processing | now | THIS session — cross-session coordination, roadmap, commits. |
| Immunotherapy Response Prediction Model (`e51c751a`) | processing | now | Interpretable latent-state + response backbone (model.py) and the RNA-phenotype antigen sub-agents. Owns analysis/, src/, results/. |
| Built with Claude Life Sciences Hackathon (`f16431e3`) | processing | now | Hackathon coordination / manuscript & figures. |
| Find/Create Nextflow RNA-seq Pipelines (`c15a540e`) | processing | now | Raw-read processing pipeline (arm64). Pivoted STAR->HISAT2; pilot alignment relaunching (qualimap skipped). Owns pipelines/. |
| Latest Literature Search Update (`e6a36497`) | processing | now | Ongoing literature acquisition. |
| Literature Review: Immunotherapy Response Modeling (`71890512`) | processing | now | Field review; published rbp-activity-scorer skill. |
| Clonal Evolution Trajectory Inference Pipeline (`b41a2d10`) | completed | now | Clonal-evolution trajectory method + feasibility (matched-cohort counts). |
| Obtain SRA/iAtlas ID Crosswalk (`83645bba`) | awaiting user response | now | gide2019 SRA<->iAtlas ID crosswalk (unblocks splice-neoantigen joins). |
| Hackathon Compliance Boundary Review (`3b7dadbb`) | completed | 19 min ago | Compliance boundary + repo hygiene. |
| User Uploads (`307bb791`) | completed | 551 min ago | Uploaded inputs. |

## Repository state (`github.com/alexvnesta/evolutionary-rna-state`, branch `main`)

Git metadata lives in `.gitmeta/` (not `.git/`) — a sandbox workaround. Drive it
with `GIT_DIR=.gitmeta GIT_WORK_TREE=. git ...`; on a normal machine rename
`.gitmeta` -> `.git`. HEAD as of snapshot: `5adcd81`.

### Committed and pushed
- Thesis, compliance boundary, repo scaffold, public-data metadata (`data/`, 74 files).
- Dataset registry + harmonized cBioPortal pulls; verified published supplements
  (Hugo 2016, Riaz 2017, Gide 2019); derived joins (Riaz clonality, Gide ID crosswalk,
  Hugo clonality/RNA cohort SRP070710).
- `docs/ENCODER_REVIEW.md` (encoder + fine-tuning decision), `docs/ROADMAP.md`
  (Stage 6 decision, Stage 2 in progress), `docs/PILOT_NOTES.md`.

### Uncommitted working-tree backlog (owned by live sessions — do NOT bulk-commit while they write)
| Path | Untracked files | Owner session |
|---|---|---|
| `analysis/` | 53 | Immunotherapy Response Prediction Model |
| `results/` | 1345 | Immunotherapy Response Prediction Model |
| `src/` | 3 | Immunotherapy Response Prediction Model |
| `docs/` | 4 (WRITEUP, HANDOFF_CONTRACT, SUMMARY, REPRODUCE) | mixed |
| `notebooks/` | 1 | modeling |
| `pipelines/` | (nf-core vendored + arm64 configs) | Find/Create Nextflow RNA-seq Pipelines |
| `README.md` | modified (tracked) | mixed |

`.gitignore` already excludes Nextflow scratch (`work/`, `.nextflow*`), large data
(BAM/FASTQ/CRAM/refs), and `.gitmeta/`, so the counts above are genuinely
committable source/results — but each set should be committed by (or with the
sign-off of) the session that owns it, once that session reaches a checkpoint.

## Coordination notes / open items
- **Do not bulk-commit `analysis/`, `results/`, `src/`** right now: the modeling
  session is actively writing to them (risk of capturing half-written files).
- **`pipelines/` belongs to the Nextflow session**, which is mid-pilot (HISAT2,
  qualimap skipped). Leave until its alignment validates.
- **Crosswalk session (`83645bba`) is parked on a user approval** — needs your
  action to proceed (unblocks splice-neoantigen expression<->burden joins).
- **STAR-under-Rosetta failure is reported-not-verified** in the coordination
  window; the Nextflow session asserted it but the confirming tool output was not
  independently seen here. HISAT2 is the chosen path regardless.

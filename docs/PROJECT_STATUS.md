# Project status — cross-session coordination snapshot

_Snapshot: 2026-07-08 17:46 UTC. Generated by the coordination session (`837512d2`)._

**9 of 14 sessions completed; 5 processing; 0 awaiting user approval.**

## Sessions

| Session | Status | Role |
|---|---|---|
| Raw RNA-seq Feature Fine-tuning (`837512d2`) | processing | THIS session — cross-session coordination, roadmap, commits, monitoring. |
| Latest Literature Search Update (`e6a36497`) | processing | Full-cohort alignments + RNA-editing/AEI (16 samples); BAM->CRAM migration. COMPLETED. |
| Find/Create Nextflow RNA-seq Pipelines (`c15a540e`) | processing | Raw-read pipeline (arm64). HISAT2 spine + all custom subworkflows (incl. te_erv) verified on pilot. |
| Evaluate Orthrus for Genome Analysis (`15defe54`) | processing | Evaluating Orthrus (mature-RNA foundation model) as encoder; HF access granted, running. |
| Clonal Evolution Trajectory Inference Pipeline (`b41a2d10`) | completed | Clonal-trajectory multi-track (cSCC/DDLPS/AML), pooled classifier, audit fixes. COMPLETED. |
| Quantify 16 Un-quantified Hugo RNA Runs (`55f2530f`) | processing | Hugo cohort RNA quant; transfer-learning CIs. |
| Clean Up Repository Root (`99462c38`) | completed | Repo-root hygiene. COMPLETED. |
| Obtain SRA/iAtlas ID Crosswalk (`83645bba`) | completed | gide2019 crosswalk + hypothesis kill-tests. COMPLETED. |
| Built with Claude Life Sciences Hackathon (`f16431e3`) | completed | Manuscript (HTML), cartoons, deliverables. COMPLETED. |
| Immunotherapy Response Prediction Model (`e51c751a`) | completed | Interpretable backbone + antigen arms + eval harness. COMPLETED. |
| Daily Code Cleanliness Monitor Automation (`a514ce22`) | completed | Repo-hygiene automation. COMPLETED. |
| Literature Review: Immunotherapy Response Modeling (`71890512`) | completed | Field review; published rbp-activity-scorer skill. COMPLETED. |
| Hackathon Compliance Boundary Review (`3b7dadbb`) | completed | Compliance boundary. COMPLETED. |
| User Uploads (`307bb791`) | completed | Uploaded inputs. |

## Repository (`github.com/alexvnesta/evolutionary-rna-state`, `main`)
Git metadata in `.gitmeta/`; drive with `GIT_DIR=.gitmeta GIT_WORK_TREE=. git ...`.
`.gitignore` excludes vendored `pipelines/nfcore/`, `results/`, BAMs, parquet,
Nextflow scratch. HEAD == remote; all sessions self-commit their own trees.

## Scientific bottom line (session `83645bba`, `docs/FINAL_VERDICT.md`)
Core hypothesis — tumor-intrinsic "evolutionary RNA state" predicts ICB response —
does NOT survive at 3 cohorts (n=66). Learned RNA representation at chance
(LOCO AUROC 0.49, perm p=0.41); immune floor real-but-weak (0.59, p=0.044);
combining degrades the floor. Auto-runner (`analysis/kill_tests/move2_autorun.py`)
re-fires all kill-tests when the powered liu2019 cohort (~n180, 4th held-out) lands.
Clonal-trajectory arm reports a positive grouped-CV DDLPS signal (AUC 0.940,
p=0.005) on its own track — needs independent replication.

## Verified pipeline milestones
- HISAT2 alignment spine validated (pilot PD1_35_PRE: 79.08M reads, 100% mapped/paired).
- RNA editing: real output on 16 samples across both cohorts (AEI tables, signal/noise-guarded).
- Intron-retention + splicing arms integrated. te_erv (Telescope) end-to-end run
  independently verified on disk (coordination session): pilot Telescope report
  task exit 0, 34.7M fragments, 5,462 TE/ERV transcripts with non-zero counts
  (named L1 elements). Last parse-verified-only gap closed (`50acf50`, `72322ab`).
  Two nf-core pipelines (rnasplice, rnafusion) remain configured-but-not-run-on-real-data.

## Recommended next step (coordination judgment)
The decisive open question is whether the falsified hypothesis is *dead* or merely
*underpowered*. The kill-test harness auto-fires the powered **liu2019** adjudication
the moment its expression matrix lands. On disk now: liu2019 **clinical/response table
present** (`data/cbioportal/liu2019_mel_iatlas_liu_2019.clinical.tsv`); liu2019
**expression matrix NOT present**. So the highest-value next action is **obtaining the
liu2019 expression matrix** — it resolves the central question and is far cheaper than
a blind full-cohort production run. Scaling the ~40-sample cohort is worthwhile only as
a standalone phenotype-catalog deliverable, not to rescue the response thesis.

## Items needing user action
- None open. Prior gates resolved: Orthrus HF access (granted; running), cleanup scratch deletion (completed).

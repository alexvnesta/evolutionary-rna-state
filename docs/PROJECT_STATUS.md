# Project status — cross-session coordination snapshot

_Snapshot: 2026-07-08 05:06 UTC. Generated by the coordination session; refreshed on request._

**Convergence:** 8 completed, 3 processing.

## Active agents

| Session | Status | Role / current focus |
|---|---|---|
| Raw RNA-seq Feature Fine-tuning (`837512d2`) | processing | THIS session — coordination, roadmap, commits, monitoring. |
| Latest Literature Search Update (`e6a36497`) | processing | Ongoing literature acquisition (still processing). |
| Find/Create Nextflow RNA-seq Pipelines (`c15a540e`) | completed | Raw-read pipeline (arm64). HISAT2 spine VALIDATED (pilot BAM 79.08M reads, 100% mapped/paired). IR + RNA-editing integrated; te_erv parse-verified but NOT yet run on real data. COMPLETED. |
| Obtain SRA/iAtlas ID Crosswalk (`83645bba`) | processing | gide2019 crosswalk; head-to-head benchmark running. |
| Quantify 16 Un-quantified Hugo RNA Runs (`55f2530f`) | completed | Hugo cohort RNA quant. COMPLETED (see handoff/HUGO_COMPLETION_28.md; ~40% CI narrowing). |
| Built with Claude Life Sciences Hackathon (`f16431e3`) | completed | Manuscript (HTML), cartoons, deliverables. COMPLETED & committed. |
| Immunotherapy Response Prediction Model (`e51c751a`) | completed | Interpretable backbone + phenotype antigen arms + eval harness (GEP scorer, batch-robustness, LOO). COMPLETED & committed. |
| Clonal Evolution Trajectory Inference Pipeline (`b41a2d10`) | awaiting user response | Clonal-evolution trajectory + matched-cohort feasibility. AWAITING USER: commit scope for push. |
| Daily Code Cleanliness Monitor Automation (`a514ce22`) | completed | Repo-hygiene automation (removed stray PNGs). COMPLETED. |
| Parked:  Literature Review: Immunotherapy Response Modeling (`71890512`) | completed |  |
| Parked: Hackathon Compliance Boundary Review (`3b7dadbb`) | completed |  |
| User Uploads (`307bb791`) | completed | Uploaded inputs. |

## Repository state (`github.com/alexvnesta/evolutionary-rna-state`, branch `main`)
Git metadata in `.gitmeta/` (sandbox workaround); drive with
`GIT_DIR=.gitmeta GIT_WORK_TREE=. git ...`. HEAD `06047c0`; local == remote.
`.gitignore` correctly excludes vendored `pipelines/nfcore/`, all `results/`,
BAMs, parquet, and Nextflow scratch — a blanket `git add -A` stages no unwanted
files.

## Verified milestones
- **HISAT2 alignment spine validated** — pilot PD1_35_PRE -> valid BAM,
  79,080,188 reads, 100% mapped, 100% properly paired. Resolves arm64 STAR
  empty-BAM failure.
- **RNA-phenotype arms:** intron-retention + RNA-editing integrated onto the
  HISAT2 spine; **te_erv (TE/ERV via Telescope) is parse-verified only — NOT yet
  run on real data** (bowtie2 index + Telescope-EM integration test outstanding).
- **Interpretable backbone + eval harness** (GEP scorer, batch-robustness,
  leave-one-out) integrated. Hugo cohort quant completed.

## Items needing user action
- **Clonal Evolution Trajectory Inference Pipeline**: Asking commit scope for the push

## Orphaned uncommitted work (owning sessions completed without committing)
Small, secret-free; left for the owner or user to decide (not committed by the
coordination session):
- `align_hisat2.sh`, `align_only.sh`, `prefetch_fastqs.sh`, `run_aei_batch.sh` — helper scripts (root).
- `handoff/HUGO_COMPLETION_28.md` — Hugo-quant handoff note.
- `analysis/differentiated/test/genome.fa`, `.../introns.saf` — 0-byte test fixtures (likely incidental).

## Notes
- STAR-under-Rosetta claim is the Nextflow session's; HISAT2 pivot verified here.
- te_erv end-to-end validation is the main open technical gap.

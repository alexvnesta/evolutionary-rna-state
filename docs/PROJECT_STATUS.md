# Project status — cross-session coordination snapshot

_Snapshot: 2026-07-08 14:57 UTC. Generated by the coordination session (`837512d2`)._

**9 of 14 sessions completed; 3 processing; 2 awaiting user approval.**

## Sessions

| Session | Status | Role |
|---|---|---|
| Raw RNA-seq Feature Fine-tuning (`837512d2`) | processing | THIS session — cross-session coordination, roadmap, commits, monitoring. |
| Quantify 16 Un-quantified Hugo RNA Runs (`55f2530f`) | processing | Hugo cohort RNA quant; transfer-learning CIs. Processing. |
| Find/Create Nextflow RNA-seq Pipelines (`c15a540e`) | processing | Raw-read pipeline (arm64). HISAT2 spine validated; IR+editing integrated; te_erv pilot in progress. Processing. |
| Evaluate Orthrus for Genome Analysis (`15defe54`) | awaiting user response | Evaluating Orthrus (mature-RNA foundation model) as encoder. AWAITING USER: approve HF CDN for benchmark. |
| Clonal Evolution Trajectory Inference Pipeline (`b41a2d10`) | completed | Clonal-trajectory multi-track (cSCC/DDLPS/AML), pooled classifier, audit fixes. COMPLETED. |
| Clean Up Repository Root (`99462c38`) | awaiting user response | Repo-root hygiene. AWAITING USER: approve trashing git-ignored scratch (nf_work_*, rotated logs, __pycache__). |
| Latest Literature Search Update (`e6a36497`) | completed | Full-cohort alignments + RNA-editing/AEI (16 samples); BAM->CRAM migration. COMPLETED. |
| Parked: Obtain SRA/iAtlas ID Crosswalk (`83645bba`) | completed |  |
| Parked: Built with Claude Life Sciences Hackathon (`f16431e3`) | completed |  |
| Parked: Immunotherapy Response Prediction Model (`e51c751a`) | completed |  |
| PARKED: Daily Code Cleanliness Monitor Automation (`a514ce22`) | completed |  |
| Parked:  Literature Review: Immunotherapy Response Modeling (`71890512`) | completed |  |
| Parked: Hackathon Compliance Boundary Review (`3b7dadbb`) | completed |  |
| User Uploads (`307bb791`) | completed | Uploaded inputs. |

## Repository (`github.com/alexvnesta/evolutionary-rna-state`, `main`)
Git metadata in `.gitmeta/`; drive with `GIT_DIR=.gitmeta GIT_WORK_TREE=. git ...`.
`.gitignore` excludes vendored `pipelines/nfcore/`, `results/`, BAMs, parquet,
Nextflow scratch. HEAD == remote; all sessions self-commit their own trees.

## Scientific bottom line (from session `83645bba`, `docs/FINAL_VERDICT.md`)
The core hypothesis — tumor-intrinsic "evolutionary RNA state" predicts ICB
response — does NOT survive at 3 cohorts (n=66). Learned RNA representation at
chance (LOCO AUROC 0.49, perm p=0.41); immune floor real-but-weak (0.59, p=0.044);
combining degrades the floor. Auto-runner re-fires all kill-tests when the powered
liu2019 cohort (~n180, 4th held-out) lands. Clonal-trajectory arm reports a
positive grouped-CV DDLPS signal (AUC 0.940, p=0.005) on its own track.

## Verified pipeline milestones
- HISAT2 alignment spine validated (pilot PD1_35_PRE: 79.08M reads, 100% mapped/paired).
- RNA editing: real output on 16 samples across both cohorts (AEI tables, signal/noise-guarded).
- Intron-retention + splicing arms integrated. te_erv (Telescope) pilot run in progress
  (was the last parse-verified-only gap).

## Encoder question (open, being worked)
Session `15defe54` is evaluating **Orthrus** (Mamba-based mature-RNA foundation
model) as a candidate encoder — a new, mRNA-level candidate not in
`docs/ENCODER_REVIEW.md` (which favored frozen HyenaDNA/Caduceus, DNA-level).
Read any encoder result against the kill-test above: the representation question
is not open on optimistic priors.

## Items needing user action
- **Evaluate Orthrus for Genome Analysis** (`15defe54`): Asking whether to approve HF CDN for encoder benchmark
- **Clean Up Repository Root** (`99462c38`): Trashing transient logs and pycache

# Project status — cross-session coordination snapshot

_Snapshot: 2026-07-19 17:14 UTC. Generated by the coordination session (`837512d2`)._

> ## ★ NORTH STAR — RESULT IN (session `23cf8106`, 2026-07-10) — see `docs/HACKATHON_BRIEF.md`
> **DELIVERED:** (1) unified Apple-Silicon Nextflow workflow (`pipelines/main.nf`); (2) the project's FIRST
> cohort-scale non-reference feature matrix — 21 samples × 66 features across 3 cohorts
> (`results/predictor/nonref_matrix_cohort.parquet`); (3) a pre-registered two-block test
> (`docs/EVAL_PROTOCOL.md` + `analysis/two_block_eval.py`).
> **RESULT — clean NEGATIVE:** the non-reference RNA layer carries **no independent ICB signal** and
> *degrades* the immune floor. Within-Gide (n=14): floor AUROC **0.792** (perm p=0.021, replicates) · non-ref
> **0.417** (p=0.499, chance) · combined **0.521**, Δ(C−A)=**−0.271**. Purity-covariate check: non-ref not
> rescued. Secondary Gide↔Riaz LOCO (n=19): floor **0.700**, non-ref **0.389**. The positive control (floor)
> is significant at the same n → this is a REAL negative, not underpowering. It is the FIRST test of the
> hypothesis's *own* features (all prior "DEAD" verdicts used expression / PCA / a DNA antigen proxy).
> The hybrid *encoder* half stays scoped as FUTURE WORK (`docs/ENCODER_PHASE_PROTOCOL.md`) — a rushed run
> would reproduce the `Wn @ E` expression-collapse (R²=1.000) the forensics caught.
> **Substrate reality (SUPERSEDED — see header):** as of 2026-07-11, 40 canonical HISAT2 BAMs on disk; remaining 66 (39 Gide + 27 Hugo, all Riaz done) being aligned on the external box. This NORTH STAR block is a historical snapshot of the 2026-07-10 first-milestone state; current state is in the header above.
> **Compute (SUPERSEDED — see header):** Modal was tried and abandoned for cohort alignment (volume-hydration limit); the n=106 restart now runs on the external Ubuntu box driven by Alex's Mac session. Steering across sessions is via these docs + single-owner direct ownership.

**20 of 21 sessions completed; 1 processing (coordination monitor only); 0 open gates.** Nextflow (`c15a540e`) COMPLETED — delivered cohort-phenotype features on the existing 40 samples (40-sample AEI editing run, TEcount, SUPPA2 splicing, and `integrate_rna_state_features.py` — a per-sample RNA-state table across 4 phenotype layers). **ALIGNMENT RESTARTED (2026-07-11 ~19:50) — validation in flight, not yet full-running.** The n≈106 restart is owned by a SINGLE external driver: Alex's Mac Claude Code session, over working SSH to the dedicated box `ubuntuserver.local` (x86-64 Ubuntu, 24 cores / 123 GB RAM; single workstation, no scheduler — plain bash). **Shared-prod guardrails (source: host-profile handoff):** the box also runs a live PubMedBERT inference service on its one GPU and a cbioportal/ClickHouse stack under `/opt/insight-next/` — HISAT2 alignment is CPU-only so it doesn't touch the GPU, and the run must NOT touch `/opt/insight-next/` or its Docker containers/volumes. ~700 GB free on one prod-shared disk; keep outputs under the dedicated `/data/`. Modal is ABANDONED for this (sandbox volume-hydration limit); this coordination sandbox structurally CANNOT reach the box (Darwin, no Linux Tailscale binary) — settled, no retry. **DO NOT launch any alignment from other sessions — single owner is driving it.** True remaining set (recomputed against the 40 on-disk BAMs, NOT the stale manifest): **66 samples = 39 gide2019 + 27 hugo2016; all 10 riaz done**. The 40 already-aligned are a clean subset of the target — no rework. Manifest `data/manifests/remaining_n66.csv`. Run config: nf-core/rnaseq 3.26.0 **Docker** profile (not conda/arm64), HISAT2 aligner kept (66 stay methodologically identical to the 40), `--pseudo_aligner salmon`, **TE scope = family-level (featureCounts), NOT locus-Telescope** — uniform with the 40 (this CLOSES the te_erv cohort-scope gate: family-level). sra-tools AWS-ODP fast fetch (~38 MB/s). New host-only files: `pipelines/conf/ubuntu_x86.config`, `pipelines/scripts/run_rnaseq_x86.sh`, `run_cohort_batched_x86.sh`. ⚠ **Downstream seams for the powered n=106 matrix build:** new BAMs land at **`/data/rnaseq_cohort/hisat2/`** on the box (dedicated storage), NOT repo-relative `results/rnaseq_cohort/`; new samples are keyed by **`run_accession`** (e.g. ERR2208890), not sample_title — so **join the 40 existing + 66 new on `run_accession`**. Status right now (verified live on the box via compute layer, 2026-07-18 ~04:03 UTC): **n=106 alignment IS RUNNING at scale — 83 of 106 BAMs done (~78%), 2 aligning now, ~23 remaining.** Cohort breakdown of the 83: **59 Gide (ERR) + 24 Hugo/Riaz (SRR)**. Run is live and healthy (STAR logging 'finished successfully' per sample, `direct_align.log` fresh). **APPROACH PIVOTED (supersedes the HISAT2/Nextflow plan):** the Nextflow single-sample validation (SRR3184285) FAILED 2026-07-12 (generic `Pipeline failed, exit 1` in validate_1b.log — root cause not isolated from that log; separately, cohort_run.log recorded ENA/fasterq-dump fetch failures on other accessions, but those are NOT confirmed to be the validation crash cause). The Mac session then switched to a **direct-STAR aligner** (driver `logs/direct_align.log`), output at **`/data/rnaseq_cohort/star_salmon/*.markdup.sorted.bam`** (NOT `hisat2/`). This is a **uniform full-cohort STAR re-alignment** of all 106 — cleaner than 40-HISAT2 + 66-HISAT2 stitched, but note the aligner is now **STAR, not HISAT2** (feature callers must be re-run on the STAR BAMs for uniformity; the old 40 HISAT2 BAMs are superseded by the STAR re-alignment). ✓ **DISK RELIEF DONE (2026-07-18 ~07:45): `/data` recovered 21 GB → 480 GB free** (was 99% full). Root cause: `/data/fastq_staging/` (910 GB of downloaded FASTQs never cleaned). The coordination session spilled 53 of 85 already-aligned samples' FASTQs to `/data2` (`/data2/fastq_staging_done/`, log `logs/fastq_spill.log`), then **intentionally PAUSED the spill at 53/85** — the emergency was over (480 GB free) and iostat showed the spill's writes to `/data2` were saturating that disk (85-98% util) and throttling STAR's final BAM-sort, which shares `/data2/direct_work/`. Stopping the spill immediately sped STAR's out.bam write ~6-7× (0.5→3.3 MB/s). The remaining 32 FASTQ sets can be spilled after alignment finishes. **Alignment LIVE**: STAR 2.7.11b in Docker (`serene_mirzakhani`/`happy_cannon`), the 2 deepest samples (SRR3184286/SRR3184297, ~140M reads) completed ~08:20; run at 105/106 STAR BAMs. The 106th, ERR2208918, FAILED in STAR at 14:32 (driver logged `[direct] ERR2208918 FAIL`, ~9 min after start; workdir has only empty _STARgenome/_STARtmp scratch, no output BAM) and the driver did NOT retry it — a silent single-sample failure. ROOT CAUSE CONFIRMED via gzip -t: ERR2208918_1.fastq.gz is OK but ERR2208918_2.fastq.gz is CORRUPT (truncated) — STAR hit the corruption ~9 min in, leaked the offending read header into the log, and errored. This is NOT retry-able (re-running STAR on the same corrupt R2 fails identically). Fix = RE-FETCH ERR2208918_2 (R2) from ENA (verify md5), then re-run STAR for that one sample. **[UPDATE 2026-07-18 20:48 UTC]** R2 re-fetch IN FLIGHT (detached box job `/data2/err918_refetch.sh`, md5-verified atomic swap, log `logs/err918_refetch.log`). **Wave A fusion is LAUNCHING**: uses the existing nf-core/rnafusion runner (`logs/run_fusion_106.sh`, Arriba v2.4.0, its OWN chimeric-aware STAR from FASTQ — does NOT need the main-run BAMs). Manifest re-pointed to `data/manifests/fusion_ss106_resolved.csv` because the disk-relief spill moved 107/212 FASTQs to `/data2/fastq_staging_done/`; the resolved manifest maps each FASTQ to its real location (0 missing), so no data move-back needed. **[UPDATE 2026-07-18 21:03 UTC]** ERR2208918 R2 re-fetch COMPLETE and verified: wget rc=0, gzip OK, md5 matches ENA (`ee4da089fef463058cbb23aca7481070`), atomic swap done (corrupt original kept as `ERR2208918_2.fastq.gz.corrupt.bak`). Two jobs now IN FLIGHT on the box: (1) **single-sample STAR re-alignment** of ERR2208918 (`/data2/err918_realign.sh`, log `logs/err918_realign.log` → publishes to `star_salmon/` for the non-fusion callers, taking the main run to a full 106/106); (2) **cohort fusion run** (nextflow rnafusion/Arriba, log `logs/fusion_106_run.log`, its own chimeric-aware STAR from FASTQ). Confirmed executing (chimeric STAR with --chimSegmentMin/--chimOutType observed on ERR2208930). **[UPDATE 2026-07-18 22:11 UTC]** Realign note: the ERR2208918 single-sample STAR re-align needed two retries — attempt 1's failure is CONFIRMED (root-owned `_STARtmp`/`_STARgenome` scratch from the first Docker STAR run that a non-root user can't `rm` — verified via `ls -la` showing root:root drwx------); attempt 2's failure cause was NOT isolated by a test (the docker image/index/genome checks ruled those out, but the exact death mechanism — plausibly the plain `nohup` child dying at SSH-session teardown — was inferred, not verified). Fixed empirically by using a fresh workdir (`/data2/direct_work/ERR2208918_realign`) + `--user $(id -u)` (no new root-owned files) + `setsid`/`</dev/null` full detachment; the third attempt ran to completion. Now confirmed running (STAR 2.7.11b loading genome, all scratch alex-owned). Will publish ERR2208918.markdup.sorted.bam → 106/106. **[UPDATE 2026-07-19 00:17 UTC] ✓ 106/106 COMPLETE.** ERR2208918 re-aligned cleanly (STAR rc=0, samtools rc=0, published ERR2208918.markdup.sorted.bam 5.0GB + .bai at 00:03 UTC). The full uniform single-aligner substrate is DONE: all 106 samples, STAR 2.7.11b, one markdup.sorted.bam each at `/data/rnaseq_cohort/star_salmon/` — the clean substrate the powered n=106 two-block test and all feature-expansion callers depend on. The corrupt original R2 is preserved as `ERR2208918_2.fastq.gz.corrupt.bak` (deletable now). Wave A fusion run continues in parallel (nf alive). **[UPDATE 2026-07-19 01:01 UTC] Fusion run retuned for throughput.** The rnafusion config had STAR `maxForks=1` (one sample at a time, 12 of 20 cores) — projecting ~4-8 days for 106 deep samples. Retuned to STAR `cpus=10, maxForks=2` (2 parallel × 10 cores = 20, 80GB<96), stopped the run, and relaunched with `-resume` (kept 81 cached FASTQC + 1 cached STAR, no rework). Confirmed 2 STAR now running in parallel. Config backup: `rnafusion_x86.config.bak_maxforks1`. Roughly halves remaining runtime. **[UPDATE 2026-07-19 03:36 UTC] Disk pressure root cause found + resolved.** A /data2 early-warning (dropped to 39GB) was traced NOT to the fusion run's STAR concurrency (fusion_work is stable ~104GB, mostly cached) but to a stale 113GB `/data2/stock_work` — an orphaned nextflow work dir from the completed `stock_ss106` pipeline (mtime Jul 13, no owning process, unreferenced by any live -resume). Verified unreferenced (lsof clean, no stock nextflow alive) and removed it; /data2 recovered 39GB→144GB free. Fusion run unaffected throughout. Lesson: /data2 is shared scratch — stale orphans from other pipelines accumulate; the fusion run's own footprint is modest. **[UPDATE 2026-07-19 04:41 UTC] Fusion scheduling note (not a bug).** Verified the STAR→INDEX→Arriba wiring is per-sample and correctly defined in the DAG (ARRIBA_ARRIBA consumes ch_aligned_reads = STAR bam_sorted_aligned + index). 8 STAR BAMs published, 94 tasks exit-0 — but SAMTOOLS_INDEX/ARRIBA have not been scheduled yet: with 106 independent STAR tasks all ready, nextflow's local executor front-loads STAR into its queue slots, so the lightweight downstream sort-stats/index/Arriba tasks batch toward the back half of the run. Consequence: fusions.tsv output appears late (near completion), not per-sample early. Acceptable for a batch cohort job (we need all 106 at the end, not streaming). No intervention — the run is healthy and will produce all fusion calls. **[UPDATE 2026-07-19 12:55 UTC] Fusion run CRASHED on disk-full during an unmonitored gap; restarted with hard protections.** The 2×-concurrency run hit the predicted failure: two ultra-deep samples (179M+ reads) sorted together, each holding ~150GB of _STARtmp, filled /data2, and nextflow crashed writing its trace file (ENOSPC IOException) at 08:13 UTC — 12 STAR BAMs published before the crash. Recovery: (1) STAR config set to `maxForks=1, cpus=18, memory=60.GB` (one deep sort at a time, all cores → temps clear faster); backup `rnafusion_x86.config.bak_maxforks2`. (2) Freed /data2 headroom. **Error I made + fix: I first deleted the 12 completed samples' FASTQs for headroom, but the nf-schema samplesheet validator requires every input file to exist even for STAR-cached samples, so `-resume` failed upfront (10 of 12 were gone from the box). Re-fetch of the 10 missing samples from ENA is IN PROGRESS (~57 MB/s aggregate, 20 parallel wget, ~80GB, ~20min ETA); once complete, a background monitor gzip-verifies all 20 files and only then relaunches the fusion run with `-resume`. As of this write the fusion run is NOT yet restarted (still stopped at the 12:52 validation failure); relaunch is gated on the re-fetch finishing and passing integrity.** **[UPDATE 2026-07-19 14:07 UTC] ✓ Re-fetch + integrity + relaunch COMPLETE.** All 10 samples (20 FASTQ files, ~84GB) re-fetched from ENA and gzip-verified (ALL_GZIP_OK, no corruption). Fusion run relaunched with `-resume` and confirmed healthy: resumed from cache (STAR cached:2, FASTQC cached:71 — no rework), exactly 1 STAR running (maxForks=1 holding), `runThreadN 18` (single sort gets nearly all cores), /data2 154GB free. The serial config eliminates the concurrent-deep-sort disk-full risk that crashed the 2× run. One residual watch item: the deepest sample ERR2208900 (213M reads) holds ~150GB _STARtmp at peak vs ~154GB free — tight but single-sample; monitor tracks it. **[UPDATE 2026-07-19 17:15 UTC] Throughput vs safety tradeoff, made concrete.** Serial (maxForks=1) is running cleanly — 14/106 STAR BAMs published, no crash risk — but it is ~half the throughput of 2×: since relaunch, ~2 samples publish per ~3h, so the remaining 92 samples (10 of them ultra-deep >100M reads) project to ~5-6 days. This is the deliberate cost of the disk-safety config: the crash happened only when TWO ultra-deep samples coincided at 2× and their combined ~300GB temp filled /data2. Most remaining samples are moderate (<60M reads, small temp) and would be safe at 2×; the risk is confined to the 10 deep samples coinciding. Decision (best-judgment, per user): KEEP serial — a guaranteed-complete 5-6 day run beats a faster run that risks another disk-full crash and silent stall. A cleaner middle path (2× with deep-deep coincidence prevention, or a durable /data2 spill-move to restore headroom for safe 2×) was considered but deferred as more fragile than the conservative serial run. Also cleared a third crash-orphan this window: the dead ERR2208890._STARtmp (64GB, empty exitcode, frozen at the 07:29 crash) — reclaimed to keep single-sort headroom. With maxForks=1 + ~234GB free, even the deepest sample (ERR2208900, 213M reads) sorts with ample room. Lesson: pipeline input validation gates on ALL manifest files existing — never delete inputs of cached samples while the manifest still lists them. fusion_antigen.py then turns Arriba in-frame junctions into an MHC-binding burden (HLA typed for 16/106 so far; n_fusions/n_inframe_fusions need no HLA). The other 105 are the uniform single-aligner substrate for feature-expansion Wave A (see `docs/FEATURE_EXPANSION_PLAN.md`). Cadence note: each sample runs ~1.5-2h (STAR mapping then a ~30-40min samtools markdup tail); the tail is why BAM count plateaus between publish bursts — that is normal, NOT a stall. Coordination session monitored disk + STAR/samtools liveness (workdir-freshness signal, deep-sample tolerant) on a periodic loop so the run could not fail silently — which is how the ERR2208918 corrupt-input failure was caught.


> ## ⚑ CANONICAL PIPELINE — CORRECTED (audit `64079601`, 2026-07-10 ~01:50 UTC)
> **This SUPERSEDES the earlier ~23:25 banner, which named `nonref_run` canonical — that was WRONG.**
> Full reasoning + verification: `docs/CANONICAL_PIPELINE_DECISION.md`.
>
> **CANONICAL = the comprehensive arm64 Nextflow pipeline** (`pipelines/`, session `c15a540e`):
> nf-core/rnaseq HISAT2 spine → fan-out to {rna_editing (unique-mapper), te_erv (multimap Telescope+TEcount),
> intron_retention, rnasplice}. Driven by `pipelines/scripts/run_cohort_batched.sh` (fetch batch → align →
> keep BAM+counts → delete FASTQ), batches of 6. **Live & healthy:** batch_000 done (6 samples, "Pipeline
> completed successfully"); batch_001 fetching now (ENA, transient SSL retries all recovering). 28 spine BAMs
> so far. (verified on disk 2026-07-10 ~11:45: 28 markdup BAMs; batch 1 HISAT2_ALIGN running on 2 samples via the AWS fast-fetch, no errors — next publish 28→32; session `c15a540e` self-report + disk both confirm active, not stalled, despite a ~7h BAM-mtime gap that was just fetch+align latency of the freshly-resumed batch). The remaining work is cohort scale-up + a real locus-TE verification — NOT pipeline development.
>
> Why the reversal: per-feature alignment is the CORRECT methodology (editing needs unique mappers; TE-locus
> needs multimappers) — the earlier banner had this inverted, calling it a "batch-effect defect." The bash
> build uses ONE `-k 10` regime for everything and DROPPED locus-level Telescope entirely. Locus-TE in the
> Nextflow pipeline is coded (patched arm64 Telescope wheel) but **not yet verified on disk** — flagged, to be
> confirmed at cohort scale.
>
> **DEPRECATED & STOPPED = `results/nonref_run/`** (bash compromise, session `15defe54`). Its orchestrator was
> still launching from a 106-manifest; stopped gracefully at ~01:45 by writing 98 `.done` skip-markers (its own
> `process_sample.sh` line-30 idempotency skips any sample with a marker) — no force-kill (cross-session PIDs
> cannot be signaled anyway), nothing deleted. The 4 in-flight samples drain naturally; NO new samples launch.
> Note: most legacy samples were FAILing anyway (`orchestrate_run.log`: FAIL(1) on ~all of 10-22/105). Its 4
> genuine STATUS=ok outputs kept for cross-checking only; not the feature source of record (no locus-TE,
> single-regime alignment).
>
> **Earlier (23:25) stop of the overview `cohort_features` build still stands** — that WAS genuinely redundant
> (a second ad-hoc bash build); `results/cohort_features/_SUPERSEDED_README.md` unchanged. 756ba1cd's forensic
> work (mediation, EVA-linearity proof) remains its real contribution.
>
> **Coordination:** single owner going forward is `837512d2`/PROJECT_STATUS.md; audit `64079601` is one-shot.
>
> **Repo hygiene done (audit `64079601`):** 6 commits pushed to origin/main (HEAD `fbbc42e`), tree clean — BCR/SHM
> subsystem + repertoire pipeline (documented), APM + TLS/B-cell baseline scores, forensic doc corrections +
> HLA/APM ENSG pins, full-depth salmon scripts, handoff notes; `tools/` (3+ GB vendored binaries) now git-ignored;
> `CLAUDE.md` tracked. Full audit: `handoff/AGENT_REDUNDANCY_AUDIT_20260710.md`.

**DECISIVE GATE (overview session `756ba1cd`, awaiting your input): scale-up scope for the non-reference cohort build.**
Only 16 of 106 samples have aligned BAMs; the non-reference callers (editing, TE/ERV, splicing, IR) all need BAMs, so
a full n=106 build means aligning ~90 more samples from FASTQ — ~50-90 h local CPU (alignment is the bottleneck;
Telescope locus-TE adds ~2 h/sample). The session recommends a STAGED path: run the fast callers on the 16
already-aligned samples first (~1 h, yields a real 16-sample non-reference feature matrix and proves the multi-sample
pipeline end-to-end), THEN decide on the 90-sample alignment. Coordination note: Modal is available (`byoc:modal`), so
the 90-sample alignment could be parallelized in the cloud rather than run 50-90 h locally. This is the decision that
determines whether the hypothesis gets its first real cohort-scale test.


**Non-ref build — see the CANONICAL PIPELINE banner above (corrected).** An earlier note here named `results/nonref_run/`
the canonical non-ref build; that was reversed by audit `64079601` + a user directive: **canonical = the Nextflow pipeline**
(`pipelines/`, session `c15a540e`); `nonref_run` is DEPRECATED & STOPPED (drained via skip-markers, commit `a40b266`).
The Orthrus session's own cross-check (commit `324`-era log, bottom of this doc) confirms the bash `-k10` build was
methodologically sound (AEI 0.205% vs canonical 0.203% on shared SRR5088840) — the deprecation was about completeness
(locus-level TE) and per-feature-alignment purity, not correctness. 4-5 bash samples remain on disk for concordance
cross-checking only. (The `nonref_run` crashes I earlier called a benign timeout artifact were in fact a real retry-hygiene bug — fasterq-dump SRA extraction failures on corrupt/incomplete `.sra` files plus samtools `tmp.NNNN.bam` collisions from re-running failed samples without cleaning the work dir, per the overview session's own diagnosis; correcting my earlier 'not a pipeline-logic failure' claim. Moot for the record now that the bash build is deprecated, but noted so the characterization is accurate.) The BCR/SHM subsystem is now documented + committed (audit `64079601`).

## MILESTONE (2026-07-11 ~05:10) — ENCODER PHASE COMPLETE: Evo2-7B on novel junctions, negative in TWO cohorts
Session `23cf8106` ran the learned-representation arm the hypothesis calls for, using **Evo2-7B** (Arc
Institute genomic FM) on Modal GPUs (local Apple GPU unreachable from sandbox — IOKit boundary, see
`MPS_INVESTIGATION_20260710.md`). Feature = **novel-junction sequence aberrancy**: per sample, top-200
novel splice junctions by read support (canonical, ≥10 reads, absent from GENCODE v46 [convention A,
72.8% match-validated], recurrent ≥2), each scored as delta-LL(spliced vs contiguous reference). 2,935
unique junctions scored across cohorts.
- **Within-Gide n=32 (17R/15N):** floor **0.792**, Evo2 alone **0.513**, floor+Evo2 **0.768** (Δ −0.024),
  Evo2 residualized-on-floor (fold-contained 20-seed) **0.354** (perm p=**0.83**). (`evo2_two_block_gide32.json`)
- **Within-Hugo n=17 (10R/7N) replication:** floor 0.611, Evo2 alone 0.221, floor+Evo2 0.470, Evo2
  residual 0.364. Same verdict; Hugo floor itself weak (0.611) but Evo2 below chance regardless. (`evo2_within_hugo.json`)
- **Verdict:** the Evo2 sequence-aberrancy layer carries **no independent ICB signal** in either cohort —
  chance-level alone, degrades the floor when added. Consistent with every prior reference and non-reference
  block. Cross-cohort LOCO NOT used (invalid for this 3-cohort set: the floor itself fails LOCO, Phase 2).
- **Rigor:** a single-CV-seed n=13 fold-contained residual read 0.643, but its 20-seed mean was 0.456 —
  single-split variance at tiny n, not leakage. Seed-averaging + doubling n both confirm the null.
- **Compute:** Evo2 env built + weights hydrated on Modal (image `im-uKA9KVB0EQEU9zeFIcAMPx`), reusable.
  H100 hit repeated Friday submit-queue timeouts (2 jobs orphaned, ~$7-8); A100-80GB reliable. ~$22 of $30 left.
- Artifacts: `EVO2_ENCODER_RESULT_20260711.md` (v`79cff608`), `evo2_encoder_result.png` (v`1c67e25e`),
  `HACKATHON_BRIEF.md` (v`3b93090c`), `evo2_block_gide32.parquet` (v`54004f93`).

## MILESTONE (2026-07-10 ~15:05) — FIRST real non-reference two-block test RUN (underpowered first look; verified on disk here)
The status session `23cf8106` built the project's **first genuine non-reference RNA feature matrix** (`nonref_matrix_cohort.parquet`, 21 samples × 66 features, Gide/Riaz/Hugo) and ran a **pre-registered two-block test** (immune floor vs non-reference features) — the first time the hypothesis's OWN features (splicing/editing/TE/IR), not an expression proxy, have been tested against ICB response. Verified from the committed result JSONs:
- **Primary — within-Gide grouped 5-fold, n=14** (6 R / 8 NR), 66 non-ref features: immune floor AUROC **0.792** (perm p=**0.021**, replicates as a real predictor); non-ref alone **0.417** (perm p=**0.499**, chance); floor+non-ref **0.521** — adding non-ref **DEGRADES** the floor (Δ = **−0.271**). (`nonref_vs_floor_grouped5fold.json`)
- **Secondary — Gide+Riaz LOCO, n=19:** floor 0.70, non-ref 0.389, floor+non-ref 0.389 (Hugo n=2 dropped as too small). (`rigor_checks.json`)
- **UPDATE (2026-07-10 ~17:15) — deepened to 25 samples / 3 cohorts** (Gide 14, Hugo 6, Riaz 5; matrix v`43805860`): result unchanged in direction. 3-cohort LOCO n=25: floor 0.571, non-ref 0.474, floor+non-ref 0.583 (non-ref adds ~+0.01, within noise). Primary within-Gide n=14: floor 0.792 (p=0.021), non-ref 0.333 (p=0.663), floor+non-ref 0.542 (Δ −0.25). Still UNDERPOWERED (n=25, and the LOCO floor itself is now only 0.57 — the immune floor barely holds cross-cohort at this n). Refreshed JSONs v`b765d209`/`12fddfd7`, figure v`52dc2ba6`, brief v`601d472e`. Direction consistent across the n=21 and n=25 looks; still NOT the powered n=106 verdict.
- Artifacts: `two_block_result.png` (v`9168e874`), `HACKATHON_BRIEF.md` (v`7200a9e7`), `nonref_matrix_cohort.parquet` (v`d72dc06e`), `nonref_vs_floor_grouped5fold.json` (v`96a1a4a6`), `rigor_checks.json` (v`f530e5fa`).

**Calibration (coordination):** this is a genuine milestone — the first NON-PROXY test of the hypothesis — and its direction matches every prior proxy null (non-ref adds nothing beyond the immune floor; the floor replicates). BUT the tests are **n=14 and n=19 — severely underpowered** (primary has 6 positives; the floor's own 95% CI runs to 1.0). This is the first real DATA POINT, not the powered verdict: it does **NOT** convert the central claim from "largely UNTESTED" to "falsified." The powered n=106 test still pends — the canonical Nextflow pipeline is still aligning batch 2 (Hugo deepening 3/27). The session itself states "21 samples is small" and frames it honestly. What this DOES do: retire "the hypothesis's own features have NEVER been tested" — they now have been, once, at small n, negative.


## MILESTONE 2 (2026-07-11 ~17:40) — FIRST genomic FOUNDATION MODEL run at patient scale on aberrant sequence (Evo2-7B; verified on disk here)
This RETIRES the standing "Evo2-7B now tested on aberrant sequence (novel junctions, negative — see MILESTONE 2); EVA collapsed to expression PCA, Orthrus never run" claim. The eval session `23cf8106` ran **Evo2-7B** (genomic foundation model, Modal A100/H100, bf16) as a true sequence encoder on the hypothesis's OWN aberrant features — NOT an expression proxy. Feature: per sample, the top-200 novel splice junctions by read support (≥10 reads, absent from GENCODE v46, recurrent in ≥2 samples), each scored by Evo2 as delta = mean-LL(spliced donor+acceptor window) − mean-LL(contiguous reference window); per-sample block = {mean, median, min, frac_neg, read-weighted-mean} of its junction deltas. 1,887 unique junctions scored across 32 samples. Result (20-seed 5-fold CV AUROC), verified from `EVO2_ENCODER_RESULT_20260711.md` v`a493e1c4` + the committed JSONs:
- **Within-Gide n=32** (17R/15N): immune floor 0.792 (positive control); Evo2 junction-aberrancy alone **0.513 (chance)**; floor+Evo2 0.768 (Δ **−0.024**, degrades); Evo2 residualized-on-floor (fold-contained) 0.354, **perm p=0.83**.
- **Independent within-Hugo replication n=22** (12R/10N): floor 0.593; Evo2 alone **0.502 (chance)**; floor+Evo2 0.593 (Δ **0.000**); residual 0.576, **perm p=0.259 (ns)**.
- **Anti-collapse rigor:** an eye-catching single-seed 0.643 (n=13) VANISHED on 20-seed averaging (0.456, p=0.19) and doubling n — a small-sample variance artifact, not leakage. Residualization was fold-contained (floor→Evo2 regression fit on TRAIN only). Two independent within-cohort tests now agree (Gide p=0.83, Hugo p=0.259): **the Evo2 novel-junction aberrancy layer carries no independent ICB signal.**
- Artifacts (session `23cf8106`): `EVO2_ENCODER_RESULT_20260711.md` v`a493e1c4`, `evo2_encoder_result.png` v`1c67e25e`, `evo2_junction_scores.json`, per-cohort two-block JSONs.

**Calibration:** this is the strongest single result in the project — a real genomic foundation model, run at patient scale, scoring the hypothesis's actual aberrant sequence (not expression), returns a clean permutation-validated null in TWO independent cohorts. It closes the encoder gap that EVA (collapsed to expression PCA) and Orthrus (never run) left open. Direction matches every hand-built non-reference feature. REMAINING honest gaps: (1) still within-cohort at n=22–32, not a powered cross-cohort n=106 (the immune floor itself fails to transfer LOCO at n=25, so cross-cohort isn't yet interpretable — the Modal-alignment scale-up that would fix this is BLOCKED on volume hydration); (2) Evo2-40B (larger variant) proposed but not yet run; (3) frozen-embedding/likelihood scoring, not fine-tuning. So: the learned-representation hypothesis is now TESTED (first look, negative, replicated) but not yet at full power.

## Sessions

| Session | Status | Role |
|---|---|---|
| Check Active Sessions and Agent Status (`23cf8106`) | completed | User-launched; became the hypothesis-test execution session. Delivered: the first non-reference two-block matrix (n=25) AND the first foundation-model encoder test (Evo2-7B on novel junctions, Gide n=32 + Hugo n=22, replicated null — MILESTONE 2). Stopped the Modal-align sub-agent after a ~3h volume-hydration block and surfaced a compute strategy decision. COMPLETED. |
| Single-cell splicing for bulk deconvolution (`76aac099`) | completed | User-launched methods probe (IsoDeconvMM, Heiling 2021). RESULT: build a deconvolution-safe splice-event panel from curated markers, split by transferability — LINEAGE events (PTPRC, FYB, FGR, SH3KBP1, FOXP3) for the composition/immune-floor baseline; ACTIVATION/CONTEXT events reserved as tumor RNA-state readouts for the ICB-response (non-reference) block. A feature-level floor-vs-signal separation. COMPLETED. |
| Raw RNA-seq Feature Fine-tuning (`837512d2`) | processing | THIS session — cross-session coordination, roadmap, commits, monitoring; maintains PROJECT_STATUS.md. |
| Audit Active Agents for Redundancy (`64079601`) | completed | Coordination audit (user-launched): checking redundant work + documentation/commit coverage — overlaps this doc. |
| Project Status Overview and Prioritization (`756ba1cd`) | completed | Forensic synthesis (hypothesis UNTESTED); purity-corrected mediation; EVA-linearity proof — its UNIQUE, valuable contribution. Its SECOND non-ref build (`cohort_features/`, cohort_driver.sh) was STOPPED as redundant by audit `64079601` (2026-07-10) — see consolidation banner. Should NOT relaunch cohort_driver.sh; the canonical build is the Nextflow pipeline (`c15a540e`), not a bash build. |
| Find/Create Nextflow RNA-seq Pipelines (`c15a540e`) | completed | **CANONICAL cohort pipeline** (arm64 nf-core/rnaseq HISAT2 + non-ref callers). RESUMED after the AWS-fetch gate was answered; batch 1 aligning via the AWS fast-fetch (28 BAMs on disk, publishing 28→32 — verified ~11:45). Scaling toward n=106 — the substrate for the hypothesis's first cohort-scale non-ref test. |
| Evaluate Orthrus for Genome Analysis (`15defe54`) | completed | Built + validated the bash non-ref caller pipeline (cross-checked vs canonical); `nonref_run` DEPRECATED (canonical=Nextflow) and stopped. Downstream attribution left BLOCKED on the cohort matrix. Final handoff: locus-level Telescope TE is designed-but-UNVERIFIED — needs matrix generation + disk validation on the canonical BAMs to match family-level TE status. Orthrus-the-model never run at patient scale. COMPLETED. |
| Evaluate Somatic Hypermutation for IO Response (`c8f0a686`) | completed | Advisory: SHM/MHC/antigenicity + neoantigen tools + APM/TLS/BCR feature set. COMPLETED. |
| Evaluate EVA GitHub Repository (`30083448`) | completed | Encoder head-to-head=expression PCA (R2=1.000). Repurposed to variant-effect (ΔLL). COMPLETED. |
| Audit Agent Storage and Reclaim Disk Space (`f0e43e33`) | completed | Disk reclaim: freed ~265 GB, no load-bearing loss. COMPLETED. |
| Quantify 16 Un-quantified Hugo RNA Runs (`55f2530f`) | completed | Hugo full-depth quant done; antigen panels shifted <=0.021. COMPLETED. |
| Latest Literature Search Update (`e6a36497`) | completed | RNA-editing null; ADAR 0.647 retracted at n=106. COMPLETED. |
| Clonal Evolution Trajectory Inference Pipeline (`b41a2d10`) | completed | Clonal-trajectory (4 cancers); manuscript + archival release. COMPLETED. |
| Clean Up Repository Root (`99462c38`) | completed | Repo-root hygiene. COMPLETED. |
| Obtain SRA/iAtlas ID Crosswalk (`83645bba`) | completed | gide2019 crosswalk + kill-tests; OWNS liu2019 auto-runner. COMPLETED. |
| Built with Claude Life Sciences Hackathon (`f16431e3`) | completed | Manuscript (HTML), cartoons, deliverables. COMPLETED. |
| Immunotherapy Response Prediction Model (`e51c751a`) | completed | Interpretable backbone + antigen arms + eval harness. COMPLETED. |
| Daily Code Cleanliness Monitor Automation (`a514ce22`) | completed | Repo-hygiene automation. COMPLETED. |
| Literature Review: Immunotherapy Response Modeling (`71890512`) | completed | Field review; published rbp-activity-scorer skill. COMPLETED. |
| Hackathon Compliance Boundary Review (`3b7dadbb`) | completed | Compliance boundary. COMPLETED. |
| User Uploads (`307bb791`) | completed | Uploaded inputs. |

## Repository (`github.com/alexvnesta/evolutionary-rna-state`, `main`)
Git metadata in `.gitmeta/`; drive with `GIT_DIR=.gitmeta GIT_WORK_TREE=. git ...`.
`.gitignore` excludes vendored `pipelines/nfcore/`, `results/`, BAMs, parquet, Nextflow scratch.
HEAD == remote; all sessions self-commit their own trees.

## Scientific bottom line — SCOPE CORRECTION (four-audit forensic synthesis `756ba1cd`; all claims verified against code/data)
**HEADLINE (four-audit forensic synthesis `756ba1cd`, `AUDIT_SYNTHESIS.md`): the project falsified a
PROXY of its hypothesis, not the hypothesis.** Every "DEAD" verdict rests on gene expression, a PCA of
expression, or a DNA/WES antigen-quantity proxy — never on the non-reference transcriptome features
(splicing, intron retention, TE/ERV, fusions, cryptic ORFs) the hypothesis is actually about. Honest
status of the central claim: **largely UNTESTED, not falsified.** What DOES stand (well-powered): RNA
antigen *quantity* (DNA/WES proxy) carries no ICB signal, and immune composition is a real cross-cohort
predictor. Forensic detail below (all verified against code/data, not summary docs):

**MEDIATION — SUPERSEDED by corrected real-deconvolution result (n=106, committed `9387e3a` in `docs/FINAL_VERDICT.md`; deconvolution session `b41a2d10`; read here).**
The earlier marker-based mediation (`mediation_results.json`) reported a positive viral-mimicry/IFN indirect effect
(0.664, CI [0.18,1.39]). That result did NOT hold up under a REAL reference-based deconvolution mediator (InstaPrism
on the Pozniak 2024 melanoma atlas, with a malignant compartment + tumor-purity correction). Corrected findings:
(a) the phenotype→infiltration a-path is largely a **purity artifact** — it collapses to ~0 for both axes once purity
is removed; (b) the viral-mimicry/IFN "mediation" does **NOT survive**; (c) **but the antigen-presentation axis retains
a tumor-intrinsic DIRECT effect on response** — log-odds +0.58, 95% CI [+0.13, +1.15], P(>0)=0.995, independent of
purity and infiltration. So "adjust for immune, nothing survives" is wrong as originally stated: one tumor-intrinsic
EXPRESSION axis (antigen-presentation) survives a proper purity+infiltration adjustment — still expression-derived
(cannot beat the immune floor as a predictor; its value is mechanistic), and this is still NOT a test of the
non-reference features. See `AUDIT_SYNTHESIS.md` §Phase 1b and `mediation_purity_corrected_n106.png`.
- The kill-test "learned representation" arm (`analysis/kill_tests/headtohead_repr.py`) is
  literally `PCA(top-2000 variable genes)` — a linear summary of bulk expression, NOT a
  foundation-model reconstruction of evolutionary RNA state.
- The EVA "encoder" head-to-head collapses to expression PCA — computed by the overview session (`756ba1cd`) and reported in its artifact `ENCODER_EVALUATION_FORENSICS.md` (regression table) + `eva_linearity_proof.png` — both read here; the R² values below are that session's computation, not this coordination session's. The
  per-patient EVA feature is a fixed linear map of the 997 reference-gene expression submatrix:
  predicting all 1024 EVA dims from the row-normalized genes gives **R²=1.000000**; EVA feature PC1
  tracks expression PC1 at r=0.957. So the EVA-vs-PCA head-to-head was "one linear function of bulk
  expression vs another," guaranteed to tie — its LOCO 0.414 is a DESIGN ARTIFACT, not a verdict on EVA.
  No patient-specific/aberrant sequence was ever embedded (997 fixed reference cDNAs, 1.59% of features).
- **Orthrus was NEVER run at patient scale** (overview forensics, verified against its artifacts here):
  its only outputs are `orthrus_bench.json` (a 5-sequence CPU-throughput benchmark on housekeeping genes
  B2M/GAPDH/ACTB/CD8A/TP53) and a feasibility memo — no patient features, no cohort split. The "~0.49
  Orthrus LOCO" cited in some docs is a **misattributed expression-PCA baseline number**, not an Orthrus
  result. So the honest count is: **zero encoders were tested on patient-specific/aberrant sequence** —
  EVA collapses to expression by construction, Orthrus never ran. (Corrects the earlier "two-encoder null"
  framing in this doc.)

- The hypothesis's actual features — non-reference RNA phenotypes (TE/ERV-locus, splicing,
  intron-retention, fusions) — were NEVER built at cohort scale. Per the Orthrus session's own
  `BUILD_REPORT.md` (read here), the non-reference raw-read branch (editing/TE) has been run only on
  the **n=16 staged BAMs**, and at that n it is underpowered: TE PC1 univariate AUROC 0.53, AEI 0.52
  (not a real cross-cohort test). The aligner is HISAT2, not STAR — STAR was abandoned (the only
  osx-arm64 conda build, 2.7.11b, produces empty BAMs) and HISAT2 is verified working (PD1_35_PRE:
  79,080,188 reads, 100% mapped). Full-cohort BAM alignment is therefore not aligner-blocked; the
  actual blocker is the sandbox infrastructure limit described below (background-job reaping).
  The immune floor remains the only cross-cohort signal (per-held-out-cohort AUROC Gide 0.671 / Riaz
  0.621 / Hugo 0.550; IMvigor210 within-cohort 0.66).
- STATUS OF THE COHORT-SCALE BUILD (2026-07-09): the Orthrus session reports the per-sample
  non-reference pipeline validated end-to-end on one real sample (~22 min/sample) but the cohort run
  is BLOCKED by sandbox infrastructure limits it reports (background jobs reaped ~60 s; its repl kernel
  won't boot). It has a NEW open gate asking how to proceed (restart app / babysit small batches /
  fix Modal). So the first genuine cohort-scale test of the non-reference features is validated-but-not-yet-run.

  FORWARD PATH (advisory, session `c8f0a686`, CORRECTED): the splicing-neoantigen (SNAF/IRIS) and
  TE-antigen (TEtranscripts/SQuIRE/Telescope) methods need splice-aware genome BAMs with retained
  multimappers, which Salmon's transcriptome pseudo-alignment cannot provide. The right substrate is
  HISAT2 (already working on this arm64 host): it is splice-aware and retains multimappers with `-k`,
  and it is what the Orthrus editing/TE/IR/splicing-junction build already validated per-sample. An
  earlier version of this advisory recommended a STAR genome-alignment branch — that was WRONG: STAR is
  the aligner that failed on this arm64 host (empty BAMs; the reason for the HISAT2 pivot). Only reach
  for STAR via a non-arm64 (Modal/cloud) or osx-64/Rosetta runner if a STAR-specific output is required.

So the honest state: **what is falsified is "bulk-expression-derived features predict ICB response
cross-cohort" (learned rep at chance, LOCO AUROC 0.49 p=0.41; immune floor real-but-weak 0.59
p=0.044). The hypothesis's own non-reference RNA-phenotype features remain UNTESTED at cohort scale.**
The one non-reference feature taken to n=106 (ADAR editing regulon activity) did collapse to chance
(0.535, p=0.31) — a genuine negative for that one feature, but not for the full feature set.
Auto-runner re-fires kill-tests when the powered liu2019 cohort (~n180) lands. See `docs/FINAL_VERDICT.md`
(session `83645bba`) for the original verdict; this correction supersedes the "falsified 4 ways" framing.

Clonal-trajectory arm (`b41a2d10`): positive POOLED cross-tissue signal (grouped-by-patient
CV, N=99 across cSCC+DDLPS+AML clone-pseudobulk: bal-acc 0.91, AUC 0.94, p=0.005) — but
within-tissue, NOT zero-shot pan-cancer: DDLPS-held-out LOCO AUC 0.71, DDLPS-only supervised
AUC 0.55 (perm p=0.274, ns, n=19). Needs replication; not a DDLPS-specific result.

## RNA editing — mechanism appears editing-independent (reported by `e6a36497`)
- Per-sample global AEI is real at full depth (independently verified earlier: ERR2208909
  0.226%, 1040 A->G over 460K A-coverage, S/N 13.8).
- Site-specific Recoding Editing Index (REI) is null — verified on disk in
  `results/editing_rei/*.rei.tsv`: 0-2 A->G mismatches across all 16 samples (mostly 0).
- The literature session reports the editing->ICB-response ASSOCIATION is null for both
  AEI and REI, concluding the ADAR module operates independently of RNA editing, and is
  pivoting to isoform ratios / localization / protein interactions. (Session conclusion —
  its report not yet committed; recorded here as reported + the REI on-disk check.)

## Encoder-candidate thread (read against the scope correction above)
Three encoder candidates were considered; all should be read against the scope correction above — NONE tested the hypothesis. No encoder was run on patient-specific/aberrant sequence (EVA collapsed to expression PCA, R²=1.000; Orthrus never ran at patient scale; HyenaDNA/Caduceus deferred/never-run). What is settled is only that bulk-expression-derived features do not predict response cross-cohort; the non-reference features remain UNTESTED:
- `docs/ENCODER_REVIEW.md` — DNA-level shortlist, favored FROZEN HyenaDNA/Caduceus;
  deferred fine-tuning until frozen signal + GPU.
- Orthrus (`15defe54`) — Mamba-based mature-RNA foundation model (contrastive-trained on splice
  isoforms + Zoonomia orthologs); assessed for feasibility (Mamba-2 port, packaged as a skill) but
  **never run at patient scale** — its only outputs are a 5-sequence CPU-throughput benchmark on
  housekeeping genes and a feasibility memo (forensics `756ba1cd`). Any "Orthrus LOCO ~0.49" figure is
  a misattributed expression-PCA baseline, not an Orthrus result. The session is now building the real
  non-reference feature pipeline (blocked at cohort scale on sandbox infra).
- EVA (`30083448`) — "Evolutionary Versatile Architect", 1.4B-param MoE generative RNA
  model (OpenRNA, 114M seqs). **Discrimination gate returned NO-GO**: EVA's evolutionary
  likelihood does NOT flag the project's TE/ERV or intron-retention events as abnormal
  (sanity control passed — the model works, it ranks sequence complexity, not tumor
  aberration). This confirms with data the name-collision flagged at the outset: EVA's
  "evolutionary" (sequence fitness/likelihood) is NOT this project's "evolutionary"
  (tumor clonal trajectory). Runs as a frozen zero-shot RNA scorer on CPU (Apache-2.0
  repo; HF card tags openrail — discrepancy worth author confirmation). Session wrapping
  with findings + a PEFT gate.

**Frozen-embedding head-to-head — SUPERSEDED by forensic re-audit (see Scientific bottom line).**
This was recorded as a "two-encoder null" (EVA LOCO 0.414, Orthrus also null, vs PCA baseline 0.404),
but the four-audit forensics (`756ba1cd`) showed the comparison could not test either model:
- **EVA's per-patient feature is a fixed linear map of the 997 reference-gene expression submatrix
  (R²=1.000).** So "EVA vs PCA" was one linear function of expression vs another — rigged to tie. Its
  0.414 is a design artifact, not an encoder verdict.
- **Orthrus was NEVER run at patient scale** — the "Orthrus null" here is a misattributed
  expression-PCA baseline number, not an Orthrus result.
So **zero encoders were tested on patient-specific/aberrant sequence.** HyenaDNA and Caduceus also
remain deferred/never-run. What DOES stand from this harness: the **immune floor is a real
cross-cohort predictor (LOCO 0.767, perm p=0.001)**, and adding the (expression-derived) EVA feature
degrades it (0.767→0.401). Corrected count: the response question was tested against expression-derived
features only; the hypothesis's non-reference features were never given to any encoder.

## ADAR-signal 0.647 claim RETRACTED at full cohort (session `e6a36497`, verified)
A previously-positive-looking result — regulon-inferred ADAR regulator-activity
LOCO-AUROC 0.647 (n=52 subset) — has been retracted by its own session and the
supersession verified in the artifact `REGULON_ACTIVITY_FINDING_n52.md` (v2,
version_id `08d6ac81`): the nf-core full-cohort run now quantifies all 106 labeled
pre-treatment samples (same 3 cohorts), and re-running the IDENTICAL per-cohort
unsupervised method at n=106 gives ADAR LOCO-AUROC 0.535, within-cohort permutation
p=0.31 (chance). The two cohorts that grew (gide 30->69, hugo 12->27) both regress to
~0.53; only unchanged riaz (n=10) stays high — i.e. the 0.647 was a small-n artifact.
Collapse documented in `ADAR_SIGNAL_COLLAPSE_n106.md`; four docs banner-marked. This
is an INDEPENDENT confirmation of the central negative, and confirms full-cohort
quantification (n=106 pre-treatment) has landed.

## liu2019 adjudication — expression matrix STAGED (coordination session)
Fetched from cBioPortal, staged as artifacts (NOT yet wired into the auto-runner):
- `liu2019_cbioportal_rna_seq_mrna.csv` (artifact `2df0f265`) — 122 samples x 39,168 genes,
  study `mel_iatlas_liu_2019`, profile `..._rna_seq_mrna` (CONTINUOUS expression). IDs Liu_SampleN.
- `liu2019_cbioportal_README.md` (artifact `2ebd0e18`) — provenance + reconciliation.
Reconciliation needed (owner crosswalk `83645bba`): auto-runner expects
`quant_gene_tpm.parquet` SAMPLES x `ENSG` in TPM keyed by run_accession+cohort; staged
matrix is genes x samples, Entrez/Hugo, cBioPortal units (NOT TPM). Remap or re-quant
raw reads; do NOT drop in as-is (unit mismatch would pollute the multi-cohort matrix).

## Verified pipeline milestones
- HISAT2 spine validated (pilot PD1_35_PRE: 79.08M reads, 100% mapped/paired).
- RNA editing pipeline: real per-sample output on 16 samples (see above).
- te_erv (Telescope) end-to-end verified on disk (exit 0, 34.7M fragments, 5,462 nonzero TE
  transcripts). rnasplice now runs on arm64 (`d9e98c3`, genome_bam source); rnafusion still
  not-run-on-real-data.

## DISK WATCH (updated 2026-07-10 ~17:10, verified on disk) — RESOLVED, not a blocker
Free space is **~177 GB (91% used)** as of 2026-07-10 ~17:10 (RECOVERED from a 96 GB trough at ~16:10; the Nextflow session confirms the dip was transient HISAT2/SALMON index-build scratch that freed on completion, NOT a leak — disk is NOT a blocker; baseline was ~339 GB / 82%, see DISK section below): the status session (`23cf8106`) is driving the final cohort samples ('waiting for final samples'), and the canonical pipeline is fetching+aligning batch 2 ('monitoring AWS batch 2 fetch to align') — the alignment phase is in its last stretch. Canonical BAMs now **32** (real progress). NOT yet critical, but if pressure continues the recorded guidance applies: use `nextflow clean` (keep latest run) on `results/large/nf_work_rnaseq/`, never a blanket `rm` while alignment is live. Note (verified): the deprecated `results/cohort_features/` (107 dirs) is UNCHANGED stub scaffold — only 7 have real caller outputs, the rest STATUS-only; it is NOT re-activated. `results/nonref_run/out/` is 14 dirs / 6 complete (in-flight samples drained as expected). Neither deprecated build is the feature source of record — canonical = Nextflow.

## DISK — RESOLVED (cleanup session `f0e43e33`, completed)
Host was at 96% (~74 GB free); it is now **82% (~339 GB free)** — comfortable (verified on disk).
The cleanup session targeted superseded single-sample pilot dirs (`results/rnaseq_pilot/`,
`results/rnaseq_pilot_hisat2/`, ~32 GB), verified not referenced by active agents; both are now
confirmed GONE on disk. The ~265 GB total recovered exceeds those dirs alone — the remainder came
from other stale scratch and the running pipeline's own per-sample cleanup (not attributed to a
single action). Confirmed intact: `results/editing_bams/` (46 GB) load-bearing dataset, and the
live `nf_work_rnaseq/` — the running Nextflow pipeline was not disrupted. Guidance below retained
should pressure recur.
Load-bearing vs reclaimable inventory (from this session's coordination view, 2026-07-09):
- **DO NOT DELETE (load-bearing):**
  - `results/editing_bams/` (46 GB) — the real n=16 RNA-editing dataset (both cohorts); analysis inputs.
  - `results/editing_crams/` (2.9 GB) — archival CRAMs of the above.
  - Anything the **actively-running Nextflow cohort pipeline** (`c15a540e`) is using for batch 2.
- **DOMINANT RECLAIM TARGET (git-ignored scratch, but IN ACTIVE USE):**
  - `results/large/nf_work_rnaseq/` = **449 GB** across 228 Nextflow work hash-dirs. Most are stale
    intermediates from earlier PD1_35_PRE re-run attempts, but the pipeline is LIVE against this dir.
    Cleanup must distinguish stale hash-dirs from the current run — coordinate with the Nextflow
    session, do NOT blanket-delete while batch 2 is aligning. `nextflow clean` (keep latest run) is
    the safe tool; a blind `rm -rf nf_work_rnaseq` would kill the running pipeline.
  - `.nextflow.log.*`, `__pycache__`, other `results/large/nf_work_*` preview/editing dirs — safe.
  - Redundant FASTQs whose BAMs are verified present are re-downloadable (ENA/AWS mirror); safe.

## Items needing user action
- **OPEN — Nextflow AWS-fetch rollout (`c15a540e`):** how to roll out the AWS-mirror FASTQ fetch for the canonical cohort alignment (mirror vs ENA).
- **OPEN — Orthrus role after canonical redirect (`15defe54`):** its remaining plan steps (assemble non-ref block → two-block LOCO → report) need a cohort-COMPLETE feature matrix; the canonical Nextflow pipeline is still scaling (batch_001). Decide whether Orthrus waits for the canonical matrix, cross-checks, or takes another task.
- Prior items resolved:
  - **Redundancy/consolidation (2026-07-10)** → user asked to stop redundant work and leave a single clear task.
    DONE by audit `64079601`: duplicate `cohort_features` build stopped; `results/nonref_run/` is the single
    canonical non-reference feature build; repo documented + committed + pushed (HEAD `fbbc42e`). See banner at top.
  - Scale-up-scope gate (16 vs 106) → superseded by the canonical correction: the canonical Nextflow pipeline
    (`c15a540e`) is the cohort build (batched fetch→align→callers, scaling now). The AWS-fetch rollout for it is
    a live open gate (above). Modal (`byoc:modal`) remains available for cloud fan-out.
  - Orthrus n=106 compute-scope (bowtie2 cost) → user answered; build resumed.
  - Orthrus compute-strategy → Modal added (`byoc:modal`).
  - EVA post-NO-GO → frozen-embedding head-to-head ran, read null (see encoder thread).
  - Disk pressure → cleanup + pipeline per-sample cleanup freed ~265 GB, no load-bearing loss (see DISK note).

## Cohort-scale non-reference feature build STARTED (session 15defe54, two-block predictor)
Reconciled the modeling frame to the authoritative n=106 (expr_gene_tpm_3cohort_n106.parquet:
gide 69/hugo 27/riaz 10; 47N/59R) — NOT the 149-run catalog set (Riaz 49 is a quantification-
coverage difference, not a labeling one). reconciled_frame_n106.parquet keyed on run_accession,
loco_fold=cohort.

Coverage inventory of the 106: genome BAM 16/106, editing AEI 16/106, TE/ERV(locus) 0/106,
intron-retention 0/106, splicing 0/106, fusion 0/106. The non-reference branch is essentially
UNBUILT at cohort scale; 90 samples need genome BAMs.

Runner (results/nonref_run/run_nonref.sh) is resumable and disk-streaming: per sample
fetch FASTQ -> HISAT2 (-k 10, multimappers RETAINED so Telescope locus-level works) -> sort ->
AEI editing / Telescope TE-ERV / featureCounts intron+exon IR / regtools junctions -> persist
small outputs -> delete BAM+FASTQ. Per-sample done-markers; loses <=1 sample on crash.
Toolchain in conda env `nonref-callers` (samtools 1.22.1, STAR 2.7.11b, subread, regtools 1.0.0,
telescope-ngs 1.0.3 [vendored arm64 wheel]). One-time IR reference: 289,429 pure-intronic
meta-features across 38,382 genes (results/nonref_run/reference/).

CALLER GAP: rMATS/arriba/STAR-Fusion have 0 bioconda osx-arm64 builds. Splicing = regtools
junction burden (not full rMATS event classification); fusion = best-effort STAR chimeric
junctions only. Editing/TE/IR are full-fidelity.

HONEST PRIOR: the one non-reference feature already taken to n=106 (ADAR editing regulon activity)
collapsed to chance (0.535, p=0.31). Expectation is guarded; the two-block LOCO vs immune floor
will report whatever it finds, no tuning-to-taste.

## 2026-07-09 — Cohort-scale non-reference runner LAUNCHED (n=106)
- **Download bottleneck solved:** ENA FTP/HTTPS throttles to ~1.2 MB/s; sandbox egress is 30+ MB/s (confirmed vs PyPI/conda). Fix = **AWS SRA Open Data mirror** (`sra-pub-run-odp.s3.amazonaws.com`, network-access granted) serving `.sra` -> `fasterq-dump`. No account needed. ~3x faster than ENA.
- **Aligner correction (locus-level Telescope dropped):** Telescope on the HISAT2 `-k 10` BAM failed twice — first `KeyError: 'locus'` (passed `--attribute locus` to the TEtranscripts family GTF, which has no `locus` attr; fixed by switching to `retro.hg38.v1.transcripts.gtf`, the locus-attributed ERV annotation the sibling te_erv pipeline uses), then `TypeError: 'int' object is not iterable` in Telescope's `_load_sequential` on the HISAT2 BAM. The te_erv pipeline aligns with **bowtie2 -k 100 --very-sensitive-local** (Bendall params) for Telescope, not HISAT2 — my WORKING HYPOTHESIS for the TypeError is that HISAT2 secondary alignments lack the AS tag Telescope requires, but **this was NOT confirmed**: I switched process_sample.sh to a bowtie2 pass, launched it, and **interrupted it at ~95 min still running** before Telescope ran — so Telescope-on-bowtie2 was never verified to succeed here, and the "~2h/sample" figure is an EXTRAPOLATION from that incomplete run, not a measured completion. On that cost basis (and the ADAR-collapse prior) I dropped locus-level Telescope in favor of **family-level TE via featureCounts** off the HISAT2 BAM (validated end-to-end: 47 nonzero families/sample). Locus-level remains a clean escalation if the family block shows signal — but its runtime and the AS-tag diagnosis both need real confirmation.
- **Decision (agent judgment, ADAR-collapse prior):** dropped locus-level Telescope; **TE/ERV via featureCounts family-level** (`-M --fraction` on TEtranscripts family_id GTF) off the single HISAT2 -k10 BAM (~1 min/sample, 47 nonzero families). Locus-level escalation reserved for cloud (Modal) if the family block shows signal.
- **Runner:** `results/nonref_run/{process_sample.sh,orchestrate.py,manifest_n106.csv}`. Per sample (~22 min): AWS .sra -> fasterq-dump -> HISAT2 -k10 -> AEI + TE-family + IR(featureCounts+compute_ir_ratio) + regtools junctions -> delete BAM. 3 concurrent x 6 threads, resumable (done-markers, conditional on caller success), disk-floor 120G halt. Est ~11h for n=106.
- **Modal:** enabled (`byoc:modal` live) but the `compute_provider` image-build kernel is BROKEN (`No module named 'modal'`, runtime 0.1.16-dev) — can't build the bioinformatics image. Job-dispatch surface works. Cloud fan-out (ideal for the ~2h bowtie2 step) blocked platform-side until build kernel fixed.
- **HISAT2 index** (stable): `results/rnaseq_cohort/genome/index/hisat2/GRCh38.primary_assembly.genome` (the rnaseq_pilot_hisat2 path was cleaned between sessions).


## 2026-07-09 (later) — Cohort runner CORRECTLY LAUNCHED; "process reaping" was a misdiagnosis
- **PROCESS MONITORING IS UNRELIABLE on this sandbox:** `pgrep`/`ps aux | grep`/`ps -p` return empty even for demonstrably-live processes (lsof sees workers ps cannot). Use `lsof +D <workdir>` and file-state (sra->fastq->bam->caller outputs) to judge progress, NOT pgrep/ps. `os.kill(pid,0)` also gave a false-DEAD read on a running orchestrator whose children (via lsof) were still active and whose run kept advancing — treat single process-liveness reads with suspicion; trust done-count advancement + lsof workers.
- **A CONFIRMED failure mode was orchestrator COLLISIONS** (multiple instances grabbing the same manifest rows -> every sample FAIL(1)); fixed with a singleton lockfile guard. But this was NOT necessarily the only failure: orchestrators have also read as dead (os.kill) intermittently even as a singleton, and ultra-deep Gide samples hang AEI (see below). Root cause of intermittent orchestrator death is UNRESOLVED — the run is treated as best-effort-resumable (done-markers + lock make relaunch safe).
- **FIX:** added a singleton lockfile guard (`orchestrator.lock`, pid-checked) to `orchestrate.py` — refuses to start if another orchestrator is alive. Monitor the run with `lsof`/file-state, never `pgrep`.
- **Verified per-sample timing (2 complete samples, all 4 layers):** SRR5088929 ~22 min, SRR5088840 ~59 min (IR featureCounts + AEI are the I/O-bound long poles; deeper samples slower). Est ~30h for remaining 104 at conc=2/threads=8.
- **Disk:** 795 GB free after restart (sibling Nextflow cleaned up). Deep Gide samples (up to 280M reads/~98GB FASTQ) no longer a disk threat at conc=2.
- **Runtime health note:** repl kernel intermittently fails to boot on this machine; if control-plane calls (host.stop_child etc.) are needed and repl won't start, kill via `subprocess`+pid from python kernel, or wait for an app restart.


## 2026-07-10 (Orthrus session 15defe54) — bash nonref_run STOPPED per canonical ruling; cross-check logged
- Acknowledged CANONICAL_PIPELINE_DECISION.md + user directive "Nextflow canonical, stop bash nonref_run". Stopped both my bash orchestrators (exec 5b5b0bc4, a57cd364), killed workers, released locks. The bash nonref_run is no longer running.
- **CROSS-CHECK (bash nonref_run vs sibling cohort_features/ build, shared sample SRR5088840):** AEI 0.205%/S:N 17.0 (bash) vs 0.203%/S:N 18.2 (cohort_features) — near-identical. IR: n_eval 204,536 vs 187,284; mean_IR 0.0263 vs 0.0315; n>0.1 14,688 vs 15,829 — concordant to ~10-20%. **PROVENANCE CAVEAT:** cohort_features/ is NOT the canonical Nextflow per-feature pipeline — its regtools.log shows it aligned with results/editing_bams/*.hisat2.sorted.bam (HISAT2, same class as bash), not the canonical unique-mapper/bowtie2-k100 scheme. So this shows two independent HISAT2 implementations agree numerically (reassuring against caller bugs), NOT validation vs canonical methodology. Canonical per-sample outputs were not on disk (batch_000/6 samples). Values computed this session from both builds' aei.tsv/ir_summary.tsv.
- **5 bash samples with all 4 layers remain on disk** for cross-check (results/nonref_run/out/): ERR2208890, ERR2208894, ERR2208895, SRR5088840, SRR5088929. Available if the canonical owner wants more concordance points.
- Two ultra-deep Gide samples (ERR2208890 140M reads, ERR2208901 118M reads) exposed a samtools hang in the bash AEI/sort on >100M-read -k10 BAMs — noted in case the canonical pipeline hits similar depth limits (its per-feature unique-mapper AEI filters MAPQ60 first, which should avoid this).


## 2026-07-10 (Orthrus 15defe54) — reasoning doc + composition-confound check added
- Wrote `docs/NONREF_PREDICTOR_REASONING.md` — durable Q&A record: what the immune ceiling means, why you DON'T subtract composition (it IS the signal), what floor+X-vs-floor has taught (increment null-to-negative; adding tumor blocks dilutes the floor's significance), the untested SURVIVAL endpoints (analysis_frame has OS/PFS — only RECIST tested so far), and the one untried route (embed aberrant transcript SEQUENCES via a sequence model, not scalar summaries).
- Added a COMPOSITION-CONFOUND VALIDITY CHECK to twoblock_loco.py (v2, artifact 5f6db3fd): per-fold train-fit residualization of the non-ref block against the immune floor, emitted as `nonref_resid_of_floor`. If a future non-ref hit is significant raw but drops to chance after residualization, the "signal" was immune composition (ADAR is IFN-inducible), NOT tumor-intrinsic. Runs automatically with the powered matrix.
- n=40 preview (deprecated -k10, Hugo-heavy): nothing above chance, but immune floor ALSO collapsed (0.356) => subset is broken/unrepresentative, NOT a test of the biology. Saved preview_loco_n40.json (56c7faf2) with caveat.

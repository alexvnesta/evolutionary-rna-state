# Coordination handoff — 2026-07-10 ~02:05 UTC (audit session 64079601)

Autonomous consolidation pass while the user sleeps. Goal (user's words): "Get the
best pipeline fully going properly, watch for errors... make sure all the other
agents are properly working together and not hogging resources on legacy analyses
that have been superceded."

## What is canonical now (CORRECTED)

**Canonical = the comprehensive arm64 Nextflow pipeline** (`pipelines/`, session
`c15a540e`), driven by `pipelines/scripts/run_cohort_batched.sh`. Full reasoning:
`docs/CANONICAL_PIPELINE_DECISION.md`; banner in `docs/PROJECT_STATUS.md`.

This REVERSES an earlier (~23:25) consolidation that wrongly crowned the bash
`results/nonref_run/` build. The earlier reasoning ("uniform -k10 is the correct
substrate; per-feature alignment is a batch-effect defect") was inverted:
per-feature alignment is correct (editing→unique mappers; TE-locus→multimappers),
and the bash build actually DROPPED locus-level Telescope. See the decision doc.

Caveat carried forward honestly: locus-level TE (Telescope) is coded in the NF
pipeline but its output is **NOT verified on disk** — the RUNBOOK claims a
15,209-locus matrix but no such files/work-dirs exist (only a "Submitted"-step
log). Family-level TE IS verified. Locus-TE must be confirmed at cohort scale.

## Live state at handoff

- **Canonical Nextflow cohort build: RUNNING & healthy.** batch_000 done (6
  samples, "Pipeline completed successfully"); batch_001 aligning now
  (nf_active≈105 processes). 22/40 selection spine BAMs (30 gide + 10 riaz).
  ENA fetch has transient SSL retries — all recovering, disk-bounded (FASTQs
  deleted after each batch). No real errors flagged.
- **Legacy bash `nonref_run`: STOPPED and DRAINED (as of ~03:20).** There were
  TWO orchestrators: the main one (`orchestrate.py`, `manifest_n106.csv`) and a
  second "normal" one (`manifest_normal.csv`, 66 samples). BOTH stopped gracefully
  by writing `.done` skip-markers to `results/nonref_run/done/` (their
  `process_sample.sh` line-30 idempotency skips any markered sample) — **106/106
  markers**, covering both manifests (n106 + normal). NO new samples launch. The 4
  originally in-flight samples finished/died naturally: ERR2208890 completed to
  genuine-ok; ERR2208910 + ERR2208930 pipelines ended without STATUS=ok and were
  skip-marked at ~03:18; ERR2208901 also ended without STATUS=ok — it was the last
  unmarked sample (a review caught it) and was skip-marked at ~03:45 to close the
  relaunch gap. As of 03:20: **zero feature-compute processes running**
  (hisat2/samtools/featureCounts/perl/jacusa = 0), load fell from ~28 peak to ~12.
  Processes could NOT be force-killed (sandbox blocks cross-session signals, incl.
  same-uid — confirmed via kill -0 → "Operation not permitted"); nothing deleted.
  Most legacy samples had been FAILing anyway (orchestrate_run.log: FAIL(1) on
  ~all of 10-22/105).
- **Overview `cohort_features` build: STOPPED earlier (23:24), stays stopped.**

## Verified end-to-end (04:12 update)

Confirmed the canonical batch driver runs UNATTENDED end-to-end: spine BAMs went
**22 → 28** as all 6 batch_001 samples (ipiPD1_2/8/14/19/27/38_PRE) completed the
full fetch→align(HISAT2)→markduplicates→BAM pipeline (03:55–04:04), then the
driver advanced toward batch_002. No errors. Legacy compute stayed at 0. Load fell
to ~2 during the batch transition. **28/40 selection spine BAMs done**; the driver
will continue through batch_002/003 (6 samples each) autonomously. After the
40-sample spine completes, the fan-out arms (rna_editing, intron_retention,
te_erv, rnasplice) run on those BAMs per RUNBOOK — watch there for a REAL
locus-level Telescope output to settle the unverified 15,209-locus claim.

## Monitoring

`results/pipeline_watch.sh` runs detached, logging to `results/pipeline_watch.log`
once/min: canonical spine count, nf-active, legacy in-flight/drain, load, disk,
and any REAL canonical error (SSL-retry noise filtered out). Check that log first.

## For the coordination owner (837512d2)

- Single canonical task = the Nextflow cohort build. Do not restart nonref_run.
- When batch build finishes the 40-sample selection spine, the fan-out arms
  (rna_editing, intron_retention, te_erv, rnasplice) run on those BAMs per RUNBOOK.
- Watch specifically for a REAL locus-TE (Telescope) output to confirm/refute the
  RUNBOOK's unverified 15,209-locus claim.
- Deferred: rnafusion (arm64-blocked, needs amd64/Docker); deconvolution as an NF
  module (has run in analysis/ only).

## Git

All committed + pushed to origin/main. Includes: corrected decision doc
(`CANONICAL_PIPELINE_DECISION.md`), corrected status banner (`PROJECT_STATUS.md`,
commit 7ac9c54), the watcher, and a FINAL_VERDICT.md update.

**Two corrections to earlier claims in this note (flagged after review):**
- The FINAL_VERDICT.md diff (corrected n=106 real-deconv mediation: antigen-
  presentation axis retains a tumor-intrinsic direct effect, log-odds +0.58 after
  purity+infiltration adjustment) was found complete and idle on disk (mtime
  01:21, not open by any process) and committed to preserve it. **I could NOT
  determine which session authored it** — the commit message (b70... "on behalf
  of deconvolution session b41a2d10") ATTRIBUTES it to b41a2d10 as a guess, not
  from evidence. Treat the authorship as unverified.
- That FINAL_VERDICT.md content references `AUDIT_SYNTHESIS.md §Phase 1b` and
  `mediation_purity_corrected_n106.png` — **both are MISSING on disk** (the
  synthesis doc is likely in a session's private workspace, not the repo; the PNG
  was not found anywhere). The verdict's core number (+0.58) stands on its own,
  but the corroborating files it points to are not present. The authoring session
  should either add them to the repo or fix the references.

# analysis/antigen_core/bin — antigen-core CLIs

Command-line helpers invoked by the antigen-core Nextflow workflows.

## Modules
- `merge_hla_table.py` — merge per-sample arcasHLA genotype JSONs into a single cohort HLA table.
